{
  "buildCommand": "cd tee && npm install --registry=https://registry.npmjs.org --no-optional && npm run build",
  "outputDirectory": "tee/.next",
  "installCommand": "echo 'Skipping install'",
  "framework": "nextjs"
}